import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.misusingdeveloper.app',
  appName: 'Mis-Using Developer',
  webDir: 'dist'
};

export default config;
